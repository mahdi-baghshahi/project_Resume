# راهنمای راه‌اندازی

## مراحل نصب

1. **نصب وابستگی‌ها:**
   ```bash
   yarn install
   ```

2. **اجرای پروژه در حالت توسعه:**
   ```bash
   yarn dev
   ```

3. **ساخت نسخه تولیدی:**
   ```bash
   yarn build
   ```

## نکات مهم

### فایل‌های استاتیک

پوشه `resources` که شامل تصاویر است، باید در پوشه `public` قرار گیرد تا در نسخه تولیدی به درستی کار کند. می‌توانید:

1. پوشه `resources` را به `public/resources` منتقل کنید، یا
2. از یک اسکریپت برای کپی کردن آن در زمان ساخت استفاده کنید

### پشتیبانی از RTL

پروژه به صورت کامل از راست‌چین (RTL) پشتیبانی می‌کند و برای زبان فارسی بهینه شده است.

### فونت Vazir

فونت Vazir از طریق CDN بارگذاری می‌شود. اگر می‌خواهید از فونت محلی استفاده کنید، می‌توانید فایل فونت را دانلود کرده و در پروژه قرار دهید.

## مشکلات رایج

### تصاویر نمایش داده نمی‌شوند

- مطمئن شوید که پوشه `resources` در مسیر صحیح قرار دارد
- در حالت توسعه، مسیر `/resources/images/...` باید کار کند
- در حالت تولید، مطمئن شوید که `resources` در `public` قرار دارد

### استایل‌ها اعمال نمی‌شوند

- مطمئن شوید که Tailwind CSS به درستی نصب شده است
- فایل `tailwind.config.js` را بررسی کنید
- دستور `yarn dev` را دوباره اجرا کنید

